<%=

config = {
  nodes: link('mysql').instances.map { |m|
      {
        host: m.address,
        api_port: 9200,
        name: m.name,
        uuid: m.id
      }
    }
  }

# JSON is valid yaml
JSON.pretty_generate(config)

%>
